spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/mivoto_local}
    username: ${DATABASE_USER:mivoto_local}
    password: ${DATABASE_PASSWORD:local_secret}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

jwt:
  secret: ${JWT_SECRET:localSecretKeyForJWTTokenGeneration123456789}
  expiration: 86400000 # 24 hours for local dev

logging:
  level:
    root: INFO
    pe.com.mivoto.service: DEBUG
    
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
